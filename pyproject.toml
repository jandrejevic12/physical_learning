[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "physical_learning"
authors = [{name = "Jovana", email = "ja539@cornell.edu"}]
dynamic = ["version", "description"]
